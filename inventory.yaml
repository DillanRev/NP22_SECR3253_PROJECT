all:
  children:
    routers:
      hosts:
        #placeholder cisco router parameter, update necessarily 
        cisco_router:
          # later, change this to the real router IP
          # only placeholder ip now (ansible_host)
          ansible_host: 127.0.0.1
          ansible_user: 2222
          ansible_password: ansible_user
          ansible_network_os: utmpassword123
          ansible_connection: ssh

    linux_servers:
      hosts:
        ubuntu-target:
          # this tells ansible to talk directly to the docker container
          ansible_connection: docker
